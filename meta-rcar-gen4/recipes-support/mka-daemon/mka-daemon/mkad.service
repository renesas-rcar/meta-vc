[Unit]
Description=MACSec Key-Agreement Service
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/mkad -c /etc/mka.d/mkad.conf
ExecStop=/bin/kill "$MAINPID"
Type=oneshot
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
