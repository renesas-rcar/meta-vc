From 6fb4c79c3786cd0a9bb2f2b7c423cf7aaf7a3bf6 Mon Sep 17 00:00:00 2001
From: Dennis Ostermann <dennis.ostermann@renesass.com>
Date: Tue, 8 Nov 2022 11:23:30 +0100
Subject: [PATCH 121/151] Re-configure RMAC if PHY reports new speed

---
 .../ethernet/renesas/rswitch2/rswitch2_eth.c  | 514 ++++++++++--------
 .../ethernet/renesas/rswitch2/rswitch2_eth.h  |   2 +-
 .../ethernet/renesas/rswitch2/rswitch2_fwd.c  |  18 +-
 drivers/net/phy/marvell_88q211x.c             |  55 +-
 4 files changed, 313 insertions(+), 276 deletions(-)

diff --git a/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.c b/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.c
index dd4a7d91ab6b..42913b0c3cb8 100644
--- a/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.c
+++ b/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.c
@@ -63,7 +63,7 @@ static int rswitch2_emac_set_state(struct net_device *ndev, enum emac_op state)
 	reg_val = FIELD_PREP(EAMC_OPC, state);
 	iowrite32(reg_val, etha_base_addr + RSW2_ETHA_EAMC);
 
-	ret = readl_poll_timeout(etha_base_addr + RSW2_ETHA_EAMS, reg_val,
+	ret = readl_poll_timeout_atomic(etha_base_addr + RSW2_ETHA_EAMS, reg_val,
 						(reg_val == FIELD_PREP(EAMS_OPS, state)),
 						RSWITCH2_REG_POLL_DELAY, RSWITCH2_REG_POLL_TIMEOUT);
 	if (ret != 0) {
@@ -156,204 +156,276 @@ static int rswitch2_gwca_init(struct rswitch2_drv *rsw2)
 	return ret;
 }
 
-static void rswitch2_phy_state_change(struct net_device *ndev)
+
+/* SerDes functionality */
+void rswitch2_serdes_write32(void __iomem *addr, u32 offs,  u32 bank, u32 data)
 {
-	struct rswitch2_drv *rsw2;
-	struct rswitch2_eth_port *eth_port;
+#ifdef DEBUG_SERDES
+	printk("%s(%d, %03x : %04x = %04x)\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, data);
+#endif
+	iowrite32(bank, addr + RSWITCH_SERDES_BANK_SELECT);
+	iowrite32(data, addr + offs);
+}
 
-	eth_port = netdev_priv(ndev);
-	rsw2 = eth_port->rsw2;
+u32 rswitch2_serdes_read32(void __iomem *addr, u32 offs,  u32 bank)
+{
+	u32 ret;
 
-	if (netif_msg_link(rsw2)) {
-		struct phy_device *phydev;
+	iowrite32(bank, addr + RSWITCH_SERDES_BANK_SELECT);
+	ret = ioread32(addr + offs);
+#ifdef DEBUG_SERDES
+	printk("%s(%d, %03x : %04x) is %04x\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, ret);
+#endif
+	return ret;
+}
 
-		phydev = ndev->phydev;
+static int rswitch2_serdes_reg_wait(void __iomem *addr, u32 offs, u32 bank, u32 mask, u32 expected)
+{
+	u32 ret;
+	int i;
 
-		phy_print_status(phydev);
+	iowrite32(bank, addr + RSWITCH_SERDES_BANK_SELECT);
+	//udelay(100);
 
-		netdev_info(ndev, "Link status changed. PHY %d UID 0x%08x Link = %d Speed = %d\n",
-					phydev->mdio.addr, phydev->phy_id, phydev->link, phydev->speed);
+	for (i = 0; i < 1000 /*RSWITCH2_REG_POLL_TIMEOUT*/; i++) {
+		ret = ioread32(addr + offs);
+		if ((ret & mask) == expected) {
+#ifdef DEBUG_SERDES
+			printk("%s(%d, %03x : %04x) is %04x after %d iterations\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, ret, i);
+#endif
+			return 0;
+		}
+		mdelay(1);
 	}
+#ifdef DEBUG_SERDES
+	printk("%s(%d, %03x : %04x) is %04x but %04x==%04x expected\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, ret, (ret&mask), expected);
+#endif
+	return -ETIMEDOUT;
 }
 
-static struct device_node *rswitch2_get_port_node(struct net_device *ndev, unsigned int port_num)
+static int rswitch2_serdes_set_speed(struct rswitch2_eth_port *eth_port)
 {
-	struct device_node *ports, *port;
-	int err = 0;
-	u32 index;
+	//int ret;
+	struct rswitch2_physical_port *phy_port;
+	void __iomem *addr = eth_port->phy_port->serdes_chan_addr;
 
-	struct rswitch2_drv *rsw2;
-	struct rswitch2_eth_port *eth_port;
+	phy_port = eth_port->phy_port;
 
-	eth_port = netdev_priv(ndev);
-	rsw2 = eth_port->rsw2;
+	printk("Set serdes speed: %d\n", phy_port->phy->speed);
 
-	//dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-	ports = of_get_child_by_name(rsw2->dev->of_node, "ports");
-	if (!ports)
-		return NULL;
+	switch (phy_port->phy_iface) {
+	case PHY_INTERFACE_MODE_SGMII:
+		if (phy_port->phy->speed == SPEED_1000)
+			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x0140);
+		else if (phy_port->phy->speed == SPEED_100)
+			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x2100);
+		else if (phy_port->phy->speed == SPEED_10)
+			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x0100);
+		else
+			return -EOPNOTSUPP;
+#if 0
+		//Autoneg for 1G
+		ret = rswitch2_serdes_reg_wait(addr, 0x008, BANK_1F00, BIT(0), 1);
+		if (ret)
+			return ret;
+		rswitch2_serdes_write32(addr, 0x008, BANK_1F00, 0);
+#endif
+		break;
 
-	//dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-	for_each_child_of_node(ports, port) {
-		err = of_property_read_u32(port, "reg", &index);
-		if (err < 0)
-			return NULL;
-		if (index == port_num) {
-			of_node_get(port);
-			break;
-		}
+	case PHY_INTERFACE_MODE_USXGMII:
+		if (phy_port->phy->speed == SPEED_2500)
+			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x120);
+		else
+			return -EOPNOTSUPP;
+	//	udelay(50);
+	//	rswitch2_serdes_write32(addr, VR_XS_PCS_DIG_CTRL1, BANK_380, 0x2600);
+	//	ret = rswitch2_serdes_reg_wait(addr, VR_XS_PCS_DIG_CTRL1, BANK_380, BIT(10), 0);
+	//	if (ret)
+	//		return ret;
+		break;
 
+	default:
+		return -EOPNOTSUPP;
 	}
-	//dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-	of_node_put(ports);
-	//dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-	dev_info(rsw2->dev, "Got port node: 0x%px\n", port);
 
-
-	return port;
+	return 0;
 }
 
-static int rswitch2_phy_init(struct net_device *ndev, unsigned int port_num)
+static int rswitch2_serdes_init(struct net_device *ndev, bool check_op);
+static void rswitch2_phy_state_change(struct net_device *ndev)
 {
 	struct rswitch2_drv *rsw2;
 	struct rswitch2_eth_port *eth_port;
 	struct rswitch2_physical_port *phy_port;
-	struct phy_device *phy = NULL;
-	struct device_node *np = ndev->dev.parent->of_node;
+	struct phy_device *phydev;
+	u32 reg_val;
 	int ret;
+	unsigned long flags;
 
 	eth_port = netdev_priv(ndev);
+	rsw2 = eth_port->rsw2;
 	phy_port = eth_port->phy_port;
+	phydev = ndev->phydev;
 
-	/* Only physical ports have PHYs attached */
-	BUG_ON(phy_port == NULL);
-	rsw2 = eth_port->rsw2;
+	if(!phydev->link) {
 
-	if (np) {
-		struct device_node *pn;
-		struct device_node *port;
-		struct mdio_device *mdiodev;
-
-		port = rswitch2_get_port_node(ndev, port_num);
-		/* Try connecting to PHY */
-		pn = of_parse_phandle(port, "phy-handle", 0);
-		if (!pn) {
-			/* In the case of a fixed PHY, the DT node associated
-			 * to the PHY is the Ethernet MAC DT node.
-			 */
-			if (of_phy_is_fixed_link(np)) {
-				ret = of_phy_register_fixed_link(np);
-				if (ret)  {
-					dev_err(rsw2->dev, "of_phy_register_fixed_link failed\n");
-					return -ENODEV;
-				}
-			}
-			pn = of_node_get(np);
-		} else {
-			dev_info(rsw2->dev, "Got phy-handle: 0x%px\n", pn);
+		void __iomem *etha_base_addr;
+
+		etha_base_addr = (void __iomem *)ndev->base_addr;
+		spin_lock_irqsave(&rsw2->lock, flags);
+		printk("===================== Link Down start =======================\n");
+
+
+
+		/* ETHA state machine will lock up due to SerDes connection in S4 ES 1.0
+		 * Lockup can be release by switching SerDes to USXGMII */
+		reg_val = FIELD_PREP(EAMC_OPC, emac_disable);
+		iowrite32(reg_val, etha_base_addr + RSW2_ETHA_EAMC);
+
+
+
+		{
+			phy_port->phy_iface = PHY_INTERFACE_MODE_USXGMII;
+			phy_port->phy->speed = SPEED_2500;
+			rswitch2_serdes_init(ndev, false);
 		}
 
-		phy = of_phy_find_device(pn);
-		if (!phy) {
-			dev_err(rsw2->dev, "of_phy_find_device(phy_np): failed\n");
+		rswitch2_emac_set_state(ndev, emac_disable);
+
+		printk("===================== Link Down end =======================\n");
+		spin_unlock_irqrestore(&rsw2->lock, flags);
+
+	} else {
+		uint phy_port_num;
+		void __iomem *etha_base_addr;
+		etha_base_addr = (void __iomem *)ndev->base_addr;
+		spin_lock_irqsave(&rsw2->lock, flags);
+
+		printk("===================== Link Up start =======================\n");
+
+
+		phy_port_num = eth_port->port_num - eth_port->rsw2->num_of_cpu_ports;
+
+		ret = rswitch2_emac_set_state(ndev, emac_config);
+		if (ret < 0) {
+			netdev_err(ndev, "Port set state 'config' failed\n");
+		}
+		else {
+			dev_err(rsw2->dev, "Port set state 'config' SUCCEEDED\n");
 		}
 
-		mdiodev = of_mdio_find_device(pn);
-		if (!mdiodev) {
-			dev_err(rsw2->dev, "No MDIO!\n");
+		reg_val = ioread32(phy_port->rmac_base_addr + RSW2_RMAC_MPIC);
+		reg_val = reg_val & ~0x1Fu;
+
+		switch (phydev->speed) {
+			case SPEED_100:
+				reg_val |= FIELD_PREP(MPIC_LSC, rsw2_rmac_100mbps);
+				reg_val |= FIELD_PREP(MPIC_PIS, rsw2_rmac_gmii);
+				phy_port->phy_iface = PHY_INTERFACE_MODE_SGMII;
+				phy_port->phy->speed = SPEED_100;
+
+				break;
+
+			case SPEED_1000:
+				reg_val |= FIELD_PREP(MPIC_LSC, rsw2_rmac_1000mbps);
+				reg_val |= FIELD_PREP(MPIC_PIS, rsw2_rmac_gmii);
+				phy_port->phy_iface = PHY_INTERFACE_MODE_SGMII;
+				phy_port->phy->speed = SPEED_1000;
+
+				break;
+
+			case SPEED_2500:
+				reg_val |= FIELD_PREP(MPIC_LSC, rsw2_rmac_2500mbps);
+				reg_val |= FIELD_PREP(MPIC_PIS, rsw2_rmac_xgmii);
+				phy_port->phy_iface = PHY_INTERFACE_MODE_USXGMII;
+				phy_port->phy->speed = SPEED_2500;
+
+				break;
+
+			default:
+				dev_err(eth_port->rsw2->dev, "Unsupported Speed\n");
+				return;
+			}
+		printk("Link change: %s uses %s at %d Mbps\n", ndev->name, phy_modes(phydev->interface), phydev->speed);
+
+		iowrite32(reg_val, phy_port->rmac_base_addr + RSW2_RMAC_MPIC);
+		printk("reg_val=0x%.8x (expected): 0x%.8x\n", ioread32(phy_port->rmac_base_addr + RSW2_RMAC_MPIC), reg_val);
+
+
+		ret = rswitch2_emac_set_state(ndev, emac_disable);
+		if (ret < 0) {
+			dev_err(rsw2->dev, "Port set state 'disable' failed\n");
+		}
+		else {
+			dev_err(rsw2->dev, "Port set state 'disable' SUCCEEDED\n");
 		}
 
-		phy = of_phy_connect(ndev, pn, &rswitch2_phy_state_change, 0 /*flags*/,
-								phy_port->phy_iface);
-		if(!phy) {
-			dev_err(rsw2->dev, "Failed to connect to Phy!\n");
-			return -EINVAL;
+
+		ret = rswitch2_emac_set_state(ndev, emac_operation);
+		if (ret < 0) {
+			dev_err(rsw2->dev, "Port set state 'operation' failed\n");
 		}
 		else {
-			dev_info(rsw2->dev, "Connected: phydev is at 0x%px\n", phy);
+			dev_err(rsw2->dev, "Port set state 'operation' SUCCEEDED\n");
 		}
-		of_node_put(pn);
-		//dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-		of_node_put(port);
-		//dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
+		rswitch2_serdes_init(ndev, true);
 
-	} else {
-#ifdef RSW2_DEPRECATED
-		/* In non device implementation, we get the port data passed this way */
-		struct rswitch2_port_data *port_data;
+		printk("===================== Link Up end =======================\n");
+		spin_unlock_irqrestore(&rsw2->lock, flags);
 
-		port_data = &rsw2->port_data[rsw2->num_of_cpu_ports + port_num];
+	}
 
-		netdev_dbg(ndev, "No device tree information. Getting PHY from ID\n");
+	if (netif_msg_link(rsw2)) {
+		int phy_speed = 0;
 
-		phy_port->phy_iface = port_data->phy_iface;
-		dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-		phy = phy_connect(ndev, port_data->phy_id, &rswitch2_phy_state_change,
-							phy_port->phy_iface);
-		dev_info(rsw2->dev, "dbg line: %d\n", __LINE__);
-#endif /* RSW2_DEPRECATED */
+		if(phydev->link)
+			phy_speed =phydev->speed;
 
-	}
+		phy_print_status(phydev);
 
-	if (IS_ERR(phy)) {
-		netdev_err(ndev, "Connect to PHY failed\n");
-		ret = PTR_ERR(phy);
-		phy = NULL;
-		return ret;
+		netdev_info(ndev, "Link status changed. PHY %d UID 0x%08x Link = %d Speed = %d\n",
+					phydev->mdio.addr, phydev->phy_id, phydev->link, phy_speed);
+	}
+	else {
+		printk("PHY state change but no msg link!\n");
 	}
-	/*netdev_dbg(ndev, "attached to PHY %d UID 0x%08x Link = %d IRQ = %d to '%s'\n",
-		   phydev->mdio.addr, phydev->phy_id, phydev->link, phydev->irq, ndev->name); */
-/*	netdev_info(ndev, "attached to PHY %d UID 0x%08x Link = %d IRQ = %d to '%s'\n",
-		   phydev->mdio.addr, phydev->phy_id, phydev->link, phydev->irq, ndev->name); */
-	return 0;
-
-}
-/* SerDes functionality */
-void rswitch2_serdes_write32(void __iomem *addr, u32 offs,  u32 bank, u32 data)
-{
-#ifdef DEBUG_SERDES
-	printk("%s(%d, %03x : %04x = %04x)\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, data);
-#endif
-	iowrite32(bank, addr + RSWITCH_SERDES_BANK_SELECT);
-	iowrite32(data, addr + offs);
 }
 
-u32 rswitch2_serdes_read32(void __iomem *addr, u32 offs,  u32 bank)
+static struct device_node *rswitch2_get_port_node(struct net_device *ndev, unsigned int port_num)
 {
-	u32 ret;
+	struct device_node *ports, *port;
+	int err = 0;
+	u32 index;
 
-	iowrite32(bank, addr + RSWITCH_SERDES_BANK_SELECT);
-	ret = ioread32(addr + offs);
-#ifdef DEBUG_SERDES
-	printk("%s(%d, %03x : %04x) is %04x\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, ret);
-#endif
-	return ret;
-}
+	struct rswitch2_drv *rsw2;
+	struct rswitch2_eth_port *eth_port;
 
-static int rswitch2_serdes_reg_wait(void __iomem *addr, u32 offs, u32 bank, u32 mask, u32 expected)
-{
-	u32 ret;
-	int i;
+	eth_port = netdev_priv(ndev);
+	rsw2 = eth_port->rsw2;
 
-	iowrite32(bank, addr + RSWITCH_SERDES_BANK_SELECT);
-	//udelay(100);
+	ports = of_get_child_by_name(rsw2->dev->of_node, "ports");
+	if (!ports)
+		return NULL;
 
-	for (i = 0; i < 1000 /*RSWITCH2_REG_POLL_TIMEOUT*/; i++) {
-		ret = ioread32(addr + offs);
-		if ((ret & mask) == expected) {
-#ifdef DEBUG_SERDES
-			printk("%s(%d, %03x : %04x) is %04x after %d iterations\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, ret, i);
-#endif
-			return 0;
+	for_each_child_of_node(ports, port) {
+		err = of_property_read_u32(port, "reg", &index);
+		if (err < 0)
+			return NULL;
+		if (index == port_num) {
+			of_node_get(port);
+			break;
 		}
-		mdelay(1);
+
 	}
-#ifdef DEBUG_SERDES
-	printk("%s(%d, %03x : %04x) is %04x but %04x==%04x expected\n", __FUNCTION__, (int)((u64)addr&0xc00)/0x400, bank, offs, ret, (ret&mask), expected);
-#endif
-	return -ETIMEDOUT;
+	of_node_put(ports);
+
+	return port;
 }
 
+
+
+
+
 static int rswitch2_serdes_init_sram(struct rswitch2_drv *rsw2)
 {
 	int ret;
@@ -647,67 +719,22 @@ static int rswitch2_serdes_chan_setting(struct rswitch2_eth_port *eth_port)
 	return 0;
 }
 
-static int rswitch2_serdes_set_speed(struct rswitch2_eth_port *eth_port)
-{
-	//int ret;
-	struct rswitch2_physical_port *phy_port;
-	void __iomem *addr = eth_port->phy_port->serdes_chan_addr;
-
-	phy_port = eth_port->phy_port;
-
-	switch (phy_port->phy_iface) {
-	case PHY_INTERFACE_MODE_SGMII:
-		if (phy_port->speed == 1000)
-			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x0140);
-		else if (phy_port->speed == 100)
-			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x2100);
-		else if (phy_port->speed == 10)
-			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x0100);
-		else
-			return -EOPNOTSUPP;
-#if 0
-		//Autoneg for 1G
-		ret = rswitch2_serdes_reg_wait(addr, 0x008, BANK_1F00, BIT(0), 1);
-		if (ret)
-			return ret;
-		rswitch2_serdes_write32(addr, 0x008, BANK_1F00, 0);
-#endif
-		break;
-
-	case PHY_INTERFACE_MODE_USXGMII:
-		if (phy_port->speed == 2500)
-			rswitch2_serdes_write32(addr, SR_MII_CTRL, BANK_1F00, 0x120);
-		else
-			return -EOPNOTSUPP;
-	//	udelay(50);
-	//	rswitch2_serdes_write32(addr, VR_XS_PCS_DIG_CTRL1, BANK_380, 0x2600);
-	//	ret = rswitch2_serdes_reg_wait(addr, VR_XS_PCS_DIG_CTRL1, BANK_380, BIT(10), 0);
-	//	if (ret)
-	//		return ret;
-		break;
-
-	default:
-		return -EOPNOTSUPP;
-	}
-
-	return 0;
-}
-
-static int dummy_config_aneg(struct phy_device *phydev)
-{
-	printk("%s:%d phydev=%p\n", __FUNCTION__, __LINE__, phydev);
-	return 0;
-}
-
 static void rswitch2_serdes_check_operation(struct timer_list *t)
 {
 	struct rswitch2_physical_port *phy_port = from_timer(phy_port, t, serdes_usxgmii_op_timer);
-//	unsigned long flags;
+	unsigned long flags;
 	u32 reg_val;
 
 
-	// FIXME: phy_port should not have its own lock - pass eth_port here
-	//spin_lock_irqsave(&phy_port->lock, flags);
+	/* FIXME: May there more elegant way to get eth_port /rsw2 data? */
+	struct rswitch2_drv *rsw2;
+	struct net_device *ndev = phy_port->phy->attached_dev;
+	struct rswitch2_eth_port *eth_port;
+
+	eth_port = netdev_priv(ndev);
+	rsw2 = eth_port->rsw2;
+
+	spin_lock_irqsave(&rsw2->lock, flags);
 
 
 	if (phy_port->serdes_usxgmii_op_cnt < RSW2_SERDES_OP_RETRIES) {
@@ -735,17 +762,16 @@ static void rswitch2_serdes_check_operation(struct timer_list *t)
 		pr_err("Could not bring SerDes USXGMII on port %s %s into operational state. Giving up!\n", phy_port->mii_bus->name, phy_port->mii_bus->id);
 	}
 
-	//spin_unlock_irqrestore(&phy_port->lock, flags);
-
+	spin_unlock_irqrestore(&rsw2->lock, flags);
 }
 
-
-static int rswitch2_serdes_init(struct net_device *ndev, unsigned int port_num)
+static int rswitch2_serdes_init(struct net_device *ndev, bool check_op)
 {
 	int ret;
 	struct rswitch2_drv *rsw2;
 	struct rswitch2_eth_port *eth_port;
 	struct rswitch2_physical_port *phy_port;
+	uint port_num;
 
 	eth_port = netdev_priv(ndev);
 	phy_port = eth_port->phy_port;
@@ -758,22 +784,19 @@ static int rswitch2_serdes_init(struct net_device *ndev, unsigned int port_num)
 	if (ret)
 		return ret;
 
+	port_num = eth_port->port_num - eth_port->rsw2->num_of_cpu_ports;
 
 	/* TODO: Support more modes and speed selection */
 	//printk("%s: sw0p%d  phy_mode=%d\n", __FUNCTION__, port_num, ndev->phydev->interface );
 	switch (phy_port->phy_iface) {
 	case PHY_INTERFACE_MODE_SGMII:
-		phy_port->speed = 1000;
-		netdev_info(ndev, "port %d uses SGMII at %d Mbit/s\n", port_num, phy_port->speed);
+		//phy_port->phy->speed = SPEED_1000;
+		netdev_info(ndev, "port %d uses SGMII at %d Mbit/s\n", port_num, phy_port->phy->speed);
 		break;
 	case PHY_INTERFACE_MODE_USXGMII:
-		phy_port->speed = 2500;
-		netdev_info(ndev, "port %d uses USXGMII at %d Mbit/s\n", port_num, phy_port->speed);
-
-		//ugly hack to prevent auto neg by generic driver
-		if (ndev->phydev->drv->config_aneg == NULL)
-			ndev->phydev->drv->config_aneg = &dummy_config_aneg;
-
+		//phy_port->phy->speed = SPEED_2500;
+		netdev_info(ndev, "port %d uses USXGMII at %d Mbit/s\n", port_num, phy_port->phy->speed);
+		printk("SerDes: phydev @0x%px\n",ndev->phydev);
 		break;
 	default:
 		pr_debug("%s: Don't support this interface %d on port %d", __func__,
@@ -823,7 +846,7 @@ static int rswitch2_serdes_init(struct net_device *ndev, unsigned int port_num)
 	rswitch2_serdes_write32(phy_port->serdes_chan_addr, 0x03d0, BANK_380, 0);
 
 	/* USXGMII needs deferred check to proof operation */
-	if(ndev->phydev->interface == PHY_INTERFACE_MODE_USXGMII) {
+	if(check_op && (phy_port->phy_iface == PHY_INTERFACE_MODE_USXGMII)) {
 		timer_setup(&phy_port->serdes_usxgmii_op_timer, rswitch2_serdes_check_operation, 0);
 		mod_timer(&phy_port->serdes_usxgmii_op_timer, jiffies + msecs_to_jiffies(RSW2_SERDES_OP_TIMER_INTERVALL_FIRST));
 	}
@@ -932,7 +955,6 @@ static int rswitch2_eth_open(struct net_device *ndev)
 	struct rswitch2_eth_port *eth_port;
 	struct rswitch2_physical_port *phy_port;
 	struct rswitch2_internal_port *intern_port;
-	unsigned int phy_port_num;
 	int ret;
 
 	eth_port = netdev_priv(ndev);
@@ -978,9 +1000,10 @@ static int rswitch2_eth_open(struct net_device *ndev)
 		netif_tx_start_all_queues(ndev);
 
 
-		phy_port_num = eth_port->port_num - eth_port->rsw2->num_of_cpu_ports;
 
-		ret = rswitch2_phy_init(ndev, phy_port_num);
+
+		ret = phy_connect_direct(ndev, phy_port->phy, &rswitch2_phy_state_change, phy_port->phy_iface);
+		//ret = rswitch2_phy_init(ndev, phy_port_num);
 		if (ret != 0) {
 			netdev_err(ndev, "rswitch2_phy_init failed: %d\n", ret);
 			return ret;
@@ -990,7 +1013,7 @@ static int rswitch2_eth_open(struct net_device *ndev)
 		//if (ret != 0)
 		//	return ret;
 
-		ret = rswitch2_serdes_init(ndev, phy_port_num);
+		ret = rswitch2_serdes_init(ndev, true);
 		if (ret != 0) {
 			netdev_err(ndev, "rswitch2_serdes_init failed: %d\n", ret);
 			return ret;
@@ -1699,7 +1722,6 @@ static irqreturn_t rswitch2_eth_interrupt(int irq, void *dev_id)
 				uint port_num;
 				uint port_q;
 				cur_queue = (reg_num * RSWITCH2_BITS_PER_REG) + reg_bit;
-				//printk("Got IRQ for Q: %d\n", cur_queue);
 
 				port_num = rsw2->port_backref[cur_queue].port_num;
 				eth_port = rsw2->ports[port_num];
@@ -2881,6 +2903,8 @@ static int rswitch2_mdio_init(struct net_device *ndev, unsigned int port_num)
 {
 	struct mii_bus *mii_bus;
 	struct device_node *dn_port;
+	struct device_node *dn_phy;
+	int phy_addr;
 	int ret;
 
 	struct rswitch2_drv *rsw2;
@@ -2909,25 +2933,50 @@ static int rswitch2_mdio_init(struct net_device *ndev, unsigned int port_num)
 	mii_bus->parent = rsw2->dev;
 
 	dn_port = rswitch2_get_port_node(ndev, port_num);
-	of_node_get(dn_port);
+	if(!dn_port) {
+		return -ENODEV;
+	}
+
+	dn_phy = of_parse_phandle(dn_port, "phy-handle", 0);
+	if(!dn_port) {
+		of_node_put(dn_port);
+		return -ENODEV;
+	}
 
 	ret = of_mdiobus_register(mii_bus, dn_port);
 	if (ret < 0) {
-		dev_info(rsw2->dev, "MDIO bus register failed: %d\n", ret);
-		mdiobus_free(mii_bus);
-		goto out;
+		dev_err(rsw2->dev, "MDIO bus register failed: %d\n", ret);
+		goto mdio_bus_free;
 	}
-	dev_info(rsw2->dev, "mii_bus->dev: '%s'\n", mii_bus->dev.init_name);
+	phy_addr = of_mdio_parse_addr(&mii_bus->dev, dn_phy);
+	//dev_info(rsw2->dev, "MDIO: Phy is at addr %d\n", phy_addr);
 
+	phy_port->phy = to_phy_device(&mii_bus->mdio_map[phy_addr]->dev);
+	if(!phy_port->phy) {
+		dev_err(rsw2->dev, "MDIO bus register, failed to get phy\n");
+		goto mdio_unreg;
+	}
+	phy_port->phy->interface = phy_port->phy_iface;
 	phy_port->mii_bus = mii_bus;
 
-out:
+	of_node_put(dn_phy);
+	of_node_put(dn_port);
+
+	return 0;
+
+
+mdio_unreg:
+	mdiobus_unregister(phy_port->mii_bus);
+
+mdio_bus_free:
+	mdiobus_free(phy_port->mii_bus);
+
+	of_node_put(dn_phy);
 	of_node_put(dn_port);
 
 	return ret;
 }
 
-
 static int rswitch2_get_phy_config(struct net_device *ndev)
 {
 	struct rswitch2_drv *rsw2;
@@ -2955,6 +3004,7 @@ static int rswitch2_get_phy_config(struct net_device *ndev)
 
 	return 0;
 }
+
 static int rswitch2_init_physical_port(struct rswitch2_drv *rsw2, unsigned int port_num)
 {
 	struct rswitch2_eth_port *eth_port;
@@ -3038,8 +3088,6 @@ static int rswitch2_init_physical_port(struct rswitch2_drv *rsw2, unsigned int p
 		goto cleanup_phy_port;
 	}
 
-
-
 	ret = rswitch2_emac_set_state(port_ndev, emac_config);
 	if (ret < 0) {
 		netdev_err(port_ndev, "Port set state 'config' failed\n");
@@ -3068,13 +3116,7 @@ static int rswitch2_init_physical_port(struct rswitch2_drv *rsw2, unsigned int p
 	rswitch2_port_set_mac_addr(port_ndev);
 	rswitch2_init_port_mac(port_ndev);
 
-	/*ret = rswitch2_emac_set_state(port_ndev, emac_disable);
-	if (ret < 0) {
-		dev_err(rsw2->dev, "Port set state 'disable' failed\n");
-		goto cleanup_phy_port;
-	}*/
-
-/* Change to OPERATION Mode */
+	/* Change to OPERATION Mode */
 	ret = rswitch2_emac_set_state(port_ndev, emac_operation);
 	if (ret < 0) {
 		netdev_err(port_ndev, "Port set state 'operation' failed\n");
@@ -3618,11 +3660,9 @@ void rswitch2_eth_exit(struct rswitch2_drv *rsw2)
 
 	while (rsw2->ports_intialized > 0) {
 		struct net_device *ndev;
-		struct phy_device *phy = ndev->phydev;
 		struct rswitch2_internal_port *intern_port;
 		struct rswitch2_physical_port *phy_port;
 
-
 		rsw2->ports_intialized--;
 
 		eth_port = rsw2->ports[rsw2->ports_intialized];
@@ -3640,8 +3680,8 @@ void rswitch2_eth_exit(struct rswitch2_drv *rsw2)
             uint num_of_rx_queues;
             uint num_of_tx_queues;
 
-            phy_device_remove(phy);
-			phy_device_free(phy);
+            phy_device_remove(phy_port->phy);
+			phy_device_free(phy_port->phy);
 
 			mdiobus_unregister(phy_port->mii_bus);
 			mdiobus_free(phy_port->mii_bus);
diff --git a/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.h b/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.h
index c897fea93b29..679a962e6868 100644
--- a/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.h
+++ b/drivers/net/ethernet/renesas/rswitch2/rswitch2_eth.h
@@ -307,8 +307,8 @@ struct rswitch2_physical_port {
 	void __iomem *rmac_base_addr;
 	void __iomem *serdes_chan_addr;
 	struct mii_bus *mii_bus;
+	struct phy_device *phy;
 	phy_interface_t phy_iface;
-	uint speed;
 
 	struct rsw2_rx_q_data rx_q[RSW2_LL_RX_PER_PORT_QUEUES];
 	struct rsw2_tx_q_data tx_q[RSW2_LL_TX_PER_PORT_QUEUES];
diff --git a/drivers/net/ethernet/renesas/rswitch2/rswitch2_fwd.c b/drivers/net/ethernet/renesas/rswitch2/rswitch2_fwd.c
index ff1cd54ed0b9..c1304685667f 100644
--- a/drivers/net/ethernet/renesas/rswitch2/rswitch2_fwd.c
+++ b/drivers/net/ethernet/renesas/rswitch2/rswitch2_fwd.c
@@ -20,7 +20,7 @@ static int rsw2_fwd_find_free_cascade_filter_slot(struct rswitch2_drv *rsw2) {
 		filter_conf = ioread32(rsw2->fwd_base_addr + RSW2_FWD_FWCFC(slot_num));
 
 		if(filter_conf == 0) {
-			printk("Cascade filter slot %d is unused\n", slot_num);
+			//printk("Cascade filter slot %d is unused\n", slot_num);
 
 			break;
 		}
@@ -46,7 +46,7 @@ static int rsw2_fwd_find_free_3byte_filter_slot(struct rswitch2_drv *rsw2) {
 		filter_val1 = ioread32(rsw2->fwd_base_addr + RSW2_FWD_FWTHBFV1C(slot_num));
 
 		if((filter_conf == 0) && (filter_val0 == 0) && (filter_val1 == 0)) {
-			printk("3byte filter slot %d is unused\n", slot_num);
+			//printk("3byte filter slot %d is unused\n", slot_num);
 
 			break;
 		}
@@ -127,17 +127,17 @@ static int rsw2_fwd_add_3byte_filter(struct rswitch2_drv *rsw2, struct three_byt
 
 	case pf_expand_mode:
 		filter_val0  = FIELD_PREP(FWTHBFV0C_THBFV0B0, filter->e.val[2]);
-		printk("filter->e.val[0]: 0x%2x filter_val0: 0x%8x\n", filter->e.val[0], filter_val0);
+	//	printk("filter->e.val[0]: 0x%2x filter_val0: 0x%8x\n", filter->e.val[0], filter_val0);
 		filter_val0 |= FIELD_PREP(FWTHBFV0C_THBFV0B1, filter->e.val[1]);
-		printk("filter->e.val[1]: 0x%2x filter_val0: 0x%8x\n", filter->e.val[1], filter_val0);
+	//	printk("filter->e.val[1]: 0x%2x filter_val0: 0x%8x\n", filter->e.val[1], filter_val0);
 		filter_val0 |= FIELD_PREP(FWTHBFV0C_THBFV0B2, filter->e.val[0]);
-		printk("filter->e.val[2]: 0x%2x filter_val0: 0x%8x\n", filter->e.val[2], filter_val0);
+	//	printk("filter->e.val[2]: 0x%2x filter_val0: 0x%8x\n", filter->e.val[2], filter_val0);
 		filter_val1  = FIELD_PREP(FWTHBFV0C_THBFV1B0, filter->e.val[5]);
-		printk("filter->e.val[3]: 0x%2x filter_val1: 0x%8x\n", filter->e.val[3], filter_val1);
+	//	printk("filter->e.val[3]: 0x%2x filter_val1: 0x%8x\n", filter->e.val[3], filter_val1);
 		filter_val1 |= FIELD_PREP(FWTHBFV0C_THBFV1B1, filter->e.val[4]);
-		printk("filter->e.val[4]: 0x%2x filter_val1: 0x%8x\n", filter->e.val[4], filter_val1);
+	//	printk("filter->e.val[4]: 0x%2x filter_val1: 0x%8x\n", filter->e.val[4], filter_val1);
 		filter_val1 |= FIELD_PREP(FWTHBFV0C_THBFV1B2, filter->e.val[3]);
-		printk("filter->e.val[5]: 0x%2x filter_val1: 0x%8x\n", filter->e.val[5], filter_val1);
+	//	printk("filter->e.val[5]: 0x%2x filter_val1: 0x%8x\n", filter->e.val[5], filter_val1);
 
 		filter->e.id = 2 * (filter_slot + RSW2_FWD_TWBF_N);
 		break;
@@ -468,7 +468,7 @@ int rswitch2_fwd_init(struct rswitch2_drv *rsw2)
 			printk("mac_pton() failed\n");
 
 		lret = rsw2_fwd_add_3byte_filter(rsw2, &filter);
-		printk("lret = %d filter.e.id = %d\n", lret, filter.e.id);
+		//printk("lret = %d filter.e.id = %d\n", lret, filter.e.id);
 
 		memset(cc_filter, 0 , sizeof(cc_filter));
 
diff --git a/drivers/net/phy/marvell_88q211x.c b/drivers/net/phy/marvell_88q211x.c
index 776addb4ecef..580a36f870a9 100644
--- a/drivers/net/phy/marvell_88q211x.c
+++ b/drivers/net/phy/marvell_88q211x.c
@@ -820,12 +820,31 @@ static int mv88q211x_config_aneg(struct phy_device *phydev)
 	bool changed = false;
 	int ret;
 
+	phydev_warn(phydev, "mv88q211x_config_aneg()\n");
+
+
 	if (phydev->autoneg == AUTONEG_DISABLE) {
 
 		/* If speed not specified, get current setting */
 		if(phydev->speed == SPEED_UNKNOWN) {
 			genphy_c45_read_pma(phydev);
+		} else if (phydev->speed == SPEED_100) {
+			phydev_warn(phydev, "Setting 100Mbit/s\n");
+			init_100mbit(phydev);
+		} else if (phydev->speed == SPEED_1000) {
+			phydev_warn(phydev, "Setting 1Gbit/s\n");
+
+			apply_setting_1gbit(phydev, MRVL_88Q2112_AN_DISABLE);
+
+			/* If the link is down, apply init script directly */
+			if (MRVL_88Q2112_SINGLE_SEND_S_ENABLE) {
+				single_send_S_1gbit(phydev);
+			}
+
+			apply_op_mode_cfg(phydev);
+			soft_reset_1gbit(phydev);
 		}
+
 		ret = genphy_c45_pma_setup_forced(phydev);
 		if(ret != 0) {
 			phydev_warn(phydev, "PMA forced setup failed: %d\n", ret);
@@ -941,37 +960,15 @@ static int mv88q211x_read_status(struct phy_device *phydev)
 	ret = mv88q211x_c45_pma_baset1_read_master_slave(phydev);
 	ret = genphy_c45_read_status(phydev);
 
+	phydev->link = check_link(phydev);
 
-	if(mv88q211x_get_speed(phydev)) {
-		 phydev->speed = 1000;
-	} else {
-		phydev->speed = 100;
-	}
-
-/*		
-	if(!phydev->link) {
-		phydev->link = check_link(phydev);
+	if(phydev->link) {
+		if(mv88q211x_get_speed(phydev)) {
+			phydev->speed = SPEED_1000;
+		} else {
+			phydev->speed = SPEED_100;
+		}
 	}
-*/
-	/*
-	 * #define MASTER_SLAVE_CFG_UNSUPPORTED		0
-#define MASTER_SLAVE_CFG_UNKNOWN		1
-#define MASTER_SLAVE_CFG_MASTER_PREFERRED	2
-#define MASTER_SLAVE_CFG_SLAVE_PREFERRED	3
-#define MASTER_SLAVE_CFG_MASTER_FORCE		4
-#define MASTER_SLAVE_CFG_SLAVE_FORCE		5
-#define MASTER_SLAVE_STATE_UNSUPPORTED		0
-#define MASTER_SLAVE_STATE_UNKNOWN		1
-#define MASTER_SLAVE_STATE_MASTER		2
-#define MASTER_SLAVE_STATE_SLAVE		3
-#define MASTER_SLAVE_STATE_ERR			4
-
-
-
-	u8 master_slave_get;
-	u8 master_slave_set;
-	u8 master_slave_state;
-*/
 
 	if(is_master(phydev))
 		phydev->master_slave_state = MASTER_SLAVE_STATE_MASTER;
-- 
2.17.1

