From 8b50eaa7c7a9818173876896db7fa23d76bcb487 Mon Sep 17 00:00:00 2001
From: "Christian@p620" <christian.mardmoeller@renesas.com>
Date: Mon, 5 Dec 2022 09:38:57 +0100
Subject: [PATCH 147/151] Import of driver version 0.6

---
 .../net/ethernet/microchip/lan865x/lan865x.c  | 130 +++++++-----------
 .../net/ethernet/microchip/lan865x/tc6-conf.h |   4 +-
 .../ethernet/microchip/lan865x/tc6-queue.h    |   0
 .../net/ethernet/microchip/lan865x/tc6-regs.c |  31 ++---
 .../net/ethernet/microchip/lan865x/tc6-regs.h |   0
 drivers/net/ethernet/microchip/lan865x/tc6.c  |  19 ++-
 drivers/net/ethernet/microchip/lan865x/tc6.h  |   9 +-
 7 files changed, 80 insertions(+), 113 deletions(-)
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/lan865x.c
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/tc6-conf.h
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/tc6-queue.h
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/tc6-regs.c
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/tc6-regs.h
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/tc6.c
 mode change 100644 => 100755 drivers/net/ethernet/microchip/lan865x/tc6.h

diff --git a/drivers/net/ethernet/microchip/lan865x/lan865x.c b/drivers/net/ethernet/microchip/lan865x/lan865x.c
old mode 100644
new mode 100755
index 65b5957486aa..dc354fb1c3fa
--- a/drivers/net/ethernet/microchip/lan865x/lan865x.c
+++ b/drivers/net/ethernet/microchip/lan865x/lan865x.c
@@ -7,6 +7,8 @@
  * Using Open Alliance TC6 Library written by Thorsten Kummermehr
  */
 
+#define DEBUG
+
 #include <linux/module.h>
 #include <linux/kernel.h>
 #include <linux/types.h>
@@ -19,7 +21,6 @@
 #include <linux/gpio.h>
 #include <linux/kthread.h>
 #include <linux/of.h>
-#include <linux/of_gpio.h>
 #include <linux/acpi.h>
 #include <linux/timer.h>
 #include <linux/jiffies.h>
@@ -28,7 +29,7 @@
 #include "tc6-regs.h"
 
 #define DRV_NAME		"LAN865x"
-#define DRV_VERSION		"0.2"
+#define DRV_VERSION		"0.6"
 
 #define REG_STDR_CONFIG0	0x1
 #define REG_STDR_STATUS0	0x2
@@ -49,13 +50,14 @@
 #define MAC_PROMISCUOUS_MODE	0x00000010
 #define MAC_MULTICAST_MODE	0x00000040
 #define MAC_UNICAST_MODE	0x00000080
-#define TX_CUT_THROUGH_MODE	0x8226
-#define TX_STORE_FWD_MODE	0x8026
+#define TX_CUT_THROUGH_MODE	0x8206
+#define TX_STORE_FWD_MODE	0x8006
 #define PLCA_ENABLE		0x8000
 #define MACPHY_HW_READY		0x80000000
 #define ETH_BUF_LEN		1536
 #define ETH_MIN_HEADER_LEN  	42
 #define TX_TIMEOUT		(4 * HZ)
+#define TC6_SECURE		(false)
 
 #define LAN865X_MSG_DEFAULT	\
 	(NETIF_MSG_PROBE | NETIF_MSG_IFUP | NETIF_MSG_IFDOWN | NETIF_MSG_LINK)
@@ -80,7 +82,6 @@ struct lan865x_priv {
 	bool tc6NeedService;
 	bool tc6IntActive;
 	uint32_t read_value;
-	int reset_gpio;
 	bool enable_data;
 	struct completion tc6_completion;
 	u8 plca_enable;
@@ -88,16 +89,20 @@ struct lan865x_priv {
 	u8 plca_node_count;
 	u8 tx_cut_thr_mode;
 	uint8_t idx;
-	struct timer_list tc6_timer;
 };
 
-#define TIMEOUT 1    //milliseconds
-
 /* use ethtool to change the level for any given device */
 static struct {
 	u32 msg_enable;
 } debug = { -1 };
 
+/* PLCA enable - Could be configured by parameter */
+static u8 plca_enable = 1;
+/* PLCA node-id - Could configured by parameter */
+static u8 plca_node_id = 0;
+/* PLCA count - Could configured by parameter */
+static u8 plca_node_count = 8;
+
 /******************************************************************************
 ** Function Name      : Tc6_Read_Callback
 ** Description        : Callback function from register read
@@ -131,7 +136,7 @@ bool Tc6_ReadRegister(struct lan865x_priv *priv, uint32_t addr, uint32_t *value)
 	long wait_remaining;
 
 	reinit_completion(&priv->tc6_completion);
-	ret = TC6_ReadRegister(priv->tc6, addr, Tc6_Read_Callback, priv);
+	ret = TC6_ReadRegister(priv->tc6, addr, TC6_SECURE, Tc6_Read_Callback, priv);
 	if (ret) {
 		priv->tc6NeedService = true;
 		wake_up_interruptible(&priv->tc6_wq);
@@ -163,7 +168,7 @@ bool Tc6_WriteRegister(struct lan865x_priv *priv, uint32_t addr, uint32_t value)
 	long wait_remaining;
 
 	reinit_completion(&priv->tc6_completion);
-	ret = TC6_WriteRegister(priv->tc6, addr, value, Tc6_Write_Callback, priv);
+	ret = TC6_WriteRegister(priv->tc6, addr, value, TC6_SECURE, Tc6_Write_Callback, priv);
 	if (ret) {
 		priv->tc6NeedService = true;
 		wake_up_interruptible(&priv->tc6_wq);
@@ -278,7 +283,8 @@ void TC6_CB_OnError(TC6_t *pInst, TC6_Error_t err, void *pGlobalTag)
 {
 	struct lan865x_priv *priv = pGlobalTag;
 
-	dev_err(&priv->spi->dev, "tc6 err = %x\n", err);
+	if (printk_ratelimit())
+		dev_err(&priv->spi->dev, "tc6 err = %x\n", err);
 }
 
 static void lan865x_spi_transfer_complete(void *context)
@@ -339,22 +345,11 @@ void TC6_CB_OnExtendedStatus(TC6_t *g, void *pGlobalTag)
 {
 	struct lan865x_priv *priv = pGlobalTag;
 
-	TC6_ReadRegister(g, REG_STDR_STATUS0, OnStatus0, priv);
+	TC6_ReadRegister(g, REG_STDR_STATUS0, TC6_SECURE, OnStatus0, priv);
 }
 
 void TC6Stub_InitializeSpi(uint8_t idx, void *pGlobalTag)
 {
-	struct lan865x_priv *priv = pGlobalTag;
-
-	if (priv->reset_gpio >= 0) {
-		gpio_request(priv->reset_gpio, "reset_gpio");
-		gpio_direction_output(priv->reset_gpio, 1);
-		gpio_set_value(priv->reset_gpio, 0);
-		mdelay(50);
-		gpio_set_value(priv->reset_gpio, 1);
-		mdelay(50);
-		gpio_free(priv->reset_gpio);
-	}
 }
 
 static bool lan865x_init(struct lan865x_priv *priv)
@@ -370,7 +365,6 @@ static bool lan865x_init(struct lan865x_priv *priv)
 		success = false;
 	}
 	if (success) {
-		i += TC6_MultipleRegisterAccess(priv->tc6, &TC6_MEMMAP[i], 1, NULL, NULL);
 		while (i < TC6_MEMMAP_LENGTH) {
 			success = Tc6_WriteRegister(priv, TC6_MEMMAP[i].address, TC6_MEMMAP[i].value);
 			if (!success) {
@@ -405,8 +399,6 @@ static bool lan865x_init(struct lan865x_priv *priv)
 		if (regVal != MACPHY_HW_READY)
 			success = false;
 	}
-	success = Tc6_ReadRegister(priv, REG_PHY_PLCA_CTRL1, &regVal);
-	pr_info("REG_PHY_PLCA_CTRL1: %x\n", regVal);
 	return success;
 }
 
@@ -627,12 +619,12 @@ static void lan865x_set_multicast_list(struct net_device *netdev)
 			else
 				hash_lo |= mask;
 		}
-		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHH, hash_hi, NULL, NULL)) {
+		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHH, hash_hi, TC6_SECURE, NULL, NULL)) {
 			if (netif_msg_timer(priv)) {
 				netdev_err(netdev, "Failed to write REG_HASHH\n");
 			}
 		}
-		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHL, hash_lo, NULL, NULL)) {
+		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHL, hash_lo, TC6_SECURE, NULL, NULL)) {
 			if (netif_msg_timer(priv)) {
 				netdev_err(netdev, "Failed to write REG_HASHL\n");
 			}
@@ -642,12 +634,12 @@ static void lan865x_set_multicast_list(struct net_device *netdev)
 		regVal |= MAC_UNICAST_MODE;
 	} else {
 		/* Enabling local MAC address only */
-		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHH, 0, NULL, NULL)) {
+		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHH, 0, TC6_SECURE, NULL, NULL)) {
 			if (netif_msg_timer(priv)) {
 				netdev_err(netdev, "Failed to write REG_HASHH\n");
 			}
 		}
-		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHL, 0, NULL, NULL)) {
+		if (!TC6_WriteRegister(priv->tc6, REG_MAC_HASHL, 0, TC6_SECURE, NULL, NULL)) {
 			if (netif_msg_timer(priv)) {
 				netdev_err(netdev, "Failed to write REG_HASHL\n");
 			}
@@ -656,7 +648,7 @@ static void lan865x_set_multicast_list(struct net_device *netdev)
 		regVal &= (~MAC_MULTICAST_MODE);
 		regVal &= (~MAC_UNICAST_MODE);
 	}
-	if (!TC6_WriteRegister(priv->tc6, REG_MAC_NW_CONFIG, regVal, NULL, NULL)) {
+	if (!TC6_WriteRegister(priv->tc6, REG_MAC_NW_CONFIG, regVal, TC6_SECURE, NULL, NULL)) {
 		if (netif_msg_timer(priv)) {
 			netdev_err(netdev, "Failed to enable promiscuous mode\n");
 		}
@@ -671,6 +663,8 @@ void OnRawTx(TC6_t *pInst, const uint8_t *pTx, uint16_t len, void *pTag, void *p
 	priv->netdev->stats.tx_packets++;
 	priv->netdev->stats.tx_bytes += len;
 	dev_kfree_skb(tx_skb);
+	if (netif_queue_stopped(priv->netdev))
+		netif_wake_queue(priv->netdev);
 }
 
 static netdev_tx_t lan865x_send_packet(struct sk_buff *skb,
@@ -680,8 +674,10 @@ static netdev_tx_t lan865x_send_packet(struct sk_buff *skb,
 	bool ret;
 
 	ret = TC6_SendRawEthernetPacket(priv->tc6, skb->data, skb->len, OnRawTx, skb);
-	if (!ret)
+	if (!ret) {
+		netif_stop_queue(priv->netdev);
 		return NETDEV_TX_BUSY;
+	}
 
 	return NETDEV_TX_OK;
 }
@@ -761,13 +757,6 @@ static int lan865x_get_of_data(struct lan865x_priv *priv)
 	if (!spi->dev.of_node)
 		return -EINVAL;
 
-	ret = of_get_named_gpio(spi->dev.of_node, "reset-pin", 0);
-	if (ret < 0 && ret != -ENOENT) {
-		dev_err(&spi->dev, "reset-pin property is not found in device tree\n");
-		return ret;
-	}
-	priv->reset_gpio = ret;
-
 	ret = of_property_read_u8(spi->dev.of_node, "plca-enable", &priv->plca_enable);
 	if (ret < 0) {
 		dev_err(&spi->dev, "plca-enable property is not found in device tree\n");
@@ -814,33 +803,16 @@ static int lan865x_get_of_data(struct lan865x_priv *priv)
 
 	return 0;
 }
-#else
-static int lan865x_get_of_data(struct lan865x_priv *priv)
-{
-	return -EINVAL;
-}
 #endif
 
 #ifdef CONFIG_ACPI
 static int lan865x_get_acpi_data(struct lan865x_priv *priv)
 {
-	priv->reset_gpio = 126;
-	priv->plca_enable = 1;
-	priv->plca_node_id = 0;
-	priv->plca_node_count = 8;
-	priv->tx_cut_thr_mode = 1;
+	priv->tx_cut_thr_mode = 0;
 
 	return 0;
 }
 #endif
-void timer_callback(struct timer_list * data)
-{
-	struct lan865x_priv *priv = from_timer(priv, data, tc6_timer);
-
-	priv->tc6IntActive = true;
-	wake_up_interruptible(&priv->tc6_wq);
-	mod_timer(&priv->tc6_timer, jiffies + msecs_to_jiffies(TIMEOUT));
-}
 
 static int lan865x_probe(struct spi_device *spi)
 {
@@ -861,6 +833,11 @@ static int lan865x_probe(struct spi_device *spi)
 	spi_set_drvdata(spi, priv);	/* spi to priv reference */
 	SET_NETDEV_DEV(netdev, &spi->dev);
 
+	/* Initialize PLCA configuration (optional from module parameter) */
+	priv->plca_enable = plca_enable;
+	priv->plca_node_id = plca_node_id;
+	priv->plca_node_count = plca_node_count;
+
 	init_completion(&priv->tc6_completion);
 	init_waitqueue_head(&priv->tc6_wq);
 
@@ -877,8 +854,7 @@ static int lan865x_probe(struct spi_device *spi)
 		goto error_platform_data;
 	}
 
-	priv->tc6_task = kthread_run(tc6_handler, priv, "tc6_task %s",
-				     dev_name(&spi->dev));
+	priv->tc6_task = kthread_run(tc6_handler, priv, "tc6_task");
 	if (IS_ERR(priv->tc6_task)) {
 		ret = PTR_ERR(priv->tc6_task);
 		goto error_tc6_task;
@@ -898,26 +874,16 @@ static int lan865x_probe(struct spi_device *spi)
 	else
 		eth_hw_addr_random(netdev);
 
-	ret = lan865x_set_hw_macaddr(netdev);
-	if (ret) {
-		if (netif_msg_probe(priv))
-			dev_err(&spi->dev, "lan865x mac addr config failed\n");
-		ret = -EIO;
-		goto error_set_mac;
-	}
-
 	spi->rt = true;
-	spi->bits_per_word = 32;
 	spi_setup(spi);
-	pr_info("spi->irq: %d  data_with: %d\n", spi->irq, spi->bits_per_word);
-	timer_setup(&priv->tc6_timer, timer_callback, 0);
-	mod_timer(&priv->tc6_timer, jiffies + msecs_to_jiffies(TIMEOUT));
+	pr_debug("spi->irq: %d\n", spi->irq);
+
 	ret = devm_request_threaded_irq(&spi->dev, spi->irq, NULL,
 					lan865x_irq, IRQF_ONESHOT,
-					dev_name(&spi->dev), priv);
-	if (ret < 0) {
+					"tc6 int", priv);
+	if ((ret != -ENOTCONN) && (ret < 0)) {
 		dev_err(&spi->dev, "error attaching irq %d\n", ret);
-		goto error_irq;
+		goto error_init;
 	}
 
 	netdev->if_port = IF_PORT_10BASET;
@@ -942,10 +908,6 @@ static int lan865x_probe(struct spi_device *spi)
 
 error_netdev_register:
 	devm_free_irq(&spi->dev, spi->irq, priv);
-error_irq:
-error_set_mac:
-	priv->enable_data = false;
-	TC6_EnableData(priv->tc6, false);
 error_init:
 	kthread_stop(priv->tc6_task);
 error_tc6_task:
@@ -959,13 +921,11 @@ static int lan865x_remove(struct spi_device *spi)
 {
 	struct lan865x_priv *priv = spi_get_drvdata(spi);
 
-	del_timer(&priv->tc6_timer);
 	unregister_netdev(priv->netdev);
-	devm_free_irq(&spi->dev, spi->irq, priv);
+	if (spi->irq > 0)
+		devm_free_irq(&spi->dev, spi->irq, priv);
 	kthread_stop(priv->tc6_task);
 	free_netdev(priv->netdev);
-	spi->rt = false;
-	spi_setup(spi);
 
 	return 0;
 }
@@ -1014,4 +974,10 @@ MODULE_AUTHOR("Thorsten Kummermehr <Thorsten.Kummermehr@microchip.com>");
 MODULE_LICENSE("GPL");
 module_param_named(debug, debug.msg_enable, int, 0);
 MODULE_PARM_DESC(debug, "Debug verbosity level in amount of bits set (0=none, ..., 31=all)");
+module_param_named(enable, plca_enable, byte, 0660);
+MODULE_PARM_DESC(enable, " PLCA Enable");
+module_param_named(node_id, plca_node_id, byte, 0660);
+MODULE_PARM_DESC(node_id, " PLCA Node-ID");
+module_param_named(node_count, plca_node_count, byte, 0660);
+MODULE_PARM_DESC(node_count, " PLCA Node-Count");
 MODULE_ALIAS("spi:" DRV_NAME);
diff --git a/drivers/net/ethernet/microchip/lan865x/tc6-conf.h b/drivers/net/ethernet/microchip/lan865x/tc6-conf.h
old mode 100644
new mode 100755
index f3b242b48a12..b4814a63f1cc
--- a/drivers/net/ethernet/microchip/lan865x/tc6-conf.h
+++ b/drivers/net/ethernet/microchip/lan865x/tc6-conf.h
@@ -51,7 +51,7 @@ extern "C" {
  * \brief Set the maximum amount of parallel TC6 instances. 1 means a single MACPHY hardware is attached
  */
 #ifndef TC6_MAX_INSTANCES
-#define TC6_MAX_INSTANCES   (3)
+#define TC6_MAX_INSTANCES   (2)
 #endif
 
 /**
@@ -83,7 +83,7 @@ extern "C" {
  * \note Given length must be power of 2 (2^n).
  */
 #ifndef REG_OP_ARRAY_SIZE
-#define REG_OP_ARRAY_SIZE   (4)
+#define REG_OP_ARRAY_SIZE   (12)
 #endif
 
 /**
diff --git a/drivers/net/ethernet/microchip/lan865x/tc6-queue.h b/drivers/net/ethernet/microchip/lan865x/tc6-queue.h
old mode 100644
new mode 100755
diff --git a/drivers/net/ethernet/microchip/lan865x/tc6-regs.c b/drivers/net/ethernet/microchip/lan865x/tc6-regs.c
old mode 100644
new mode 100755
index 71279642e16a..55a95cfbe2dd
--- a/drivers/net/ethernet/microchip/lan865x/tc6-regs.c
+++ b/drivers/net/ethernet/microchip/lan865x/tc6-regs.c
@@ -41,25 +41,24 @@
 /*>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/
 
 const MemoryMap_t TC6_MEMMAP[] = {
-    {  .address = 0x00000001,  .value=0x00000026,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
-    {  .address = 0x00000001,  .value=0x00000026,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x000130D0,  .value=0x000030EA,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x000130D1,  .value=0x00000800,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
+    {  .address = 0x00000001,  .value=0x00004000,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x000130D0,  .value=0x000030EA,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x000130D1,  .value=0x00000800,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
 #if (ENABLE_COLLISION_DETECTION)
-    {  .address = 0x00013084,  .value=0x0000338F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x00013087,  .value=0x0000901F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
+    {  .address = 0x00013084,  .value=0x0000338F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x00013087,  .value=0x0000901F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
 #else
-    {  .address = 0x00013084,  .value=0x0000320F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x00013087,  .value=0x0000101F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
+    {  .address = 0x00013084,  .value=0x0000320F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x00013087,  .value=0x0000101F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
 #endif
-    {  .address = 0x00013085,  .value=0x00000006,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x0001308A,  .value=0x0000C0FF,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x00013088,  .value=0x0000633F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x0001308B,  .value=0x00000404,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x00013080,  .value=0x00000680,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x000130F1,  .value=0x00002440,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x00013096,  .value=0x00002000,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
-    {  .address = 0x00013099,  .value=0x00007F80,  .mask=0x00000000,  .op=MemOp_Write,  .secure=true   },
+    {  .address = 0x00013085,  .value=0x00000006,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x0001308A,  .value=0x0000C0FF,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x00013088,  .value=0x0000633F,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x0001308B,  .value=0x00000404,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x00013080,  .value=0x00000680,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x000130F1,  .value=0x00002440,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x00013096,  .value=0x00002000,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
+    {  .address = 0x00013099,  .value=0x00007F80,  .mask=0x00000000,  .op=MemOp_Write,  .secure=false  },
 };
 
 const uint32_t TC6_MEMMAP_LENGTH = (sizeof(TC6_MEMMAP) / sizeof(MemoryMap_t));
diff --git a/drivers/net/ethernet/microchip/lan865x/tc6-regs.h b/drivers/net/ethernet/microchip/lan865x/tc6-regs.h
old mode 100644
new mode 100755
diff --git a/drivers/net/ethernet/microchip/lan865x/tc6.c b/drivers/net/ethernet/microchip/lan865x/tc6.c
old mode 100644
new mode 100755
index 0295b8c0fa95..9a30fd9a61c4
--- a/drivers/net/ethernet/microchip/lan865x/tc6.c
+++ b/drivers/net/ethernet/microchip/lan865x/tc6.c
@@ -215,7 +215,6 @@ TC6_t *TC6_Init(void *pGlobalTag)
             g->prot.txc = 24;
             g->gTag = pGlobalTag;
             init_qtxeth_queue(&g->prot.eth_q, g->prot.tx_eth_buffer, TC6_TX_ETH_QSIZE);
-            TC6Stub_InitializeSpi(g->instance, g->gTag);
             init_qspibuf_queue(&g->qSpi, g->spiBuf, SPI_FULL_BUFFERS);
             init_regop_queue(&g->regop_q, g->regop_storage, REG_OP_ARRAY_SIZE);
             break;
@@ -343,31 +342,31 @@ bool TC6_SendRawEthernetPacket(TC6_t *g, const uint8_t *pTx, uint16_t len, TC6_R
     return success;
 }
 
-bool TC6_ReadRegister(TC6_t *g, uint32_t addr, TC6_RegCallback_t rxCallback, void *tag)
+bool TC6_ReadRegister(TC6_t *g, uint32_t addr, bool secure, TC6_RegCallback_t rxCallback, void *tag)
 {
     return accessRegisters(g, REGISTER_OP_READ, addr
         , 0    /* value */
-        , true /* protected */
+        , secure
         , 0    /* mask */
         , rxCallback
         , tag);
 }
 
-bool TC6_WriteRegister(TC6_t *g, uint32_t addr, uint32_t value, TC6_RegCallback_t txCallback, void *tag)
+bool TC6_WriteRegister(TC6_t *g, uint32_t addr, uint32_t value, bool secure, TC6_RegCallback_t txCallback, void *tag)
 {
     return accessRegisters(g, REGISTER_OP_WRITE, addr
         , value
-        , true /* protected */
+        , secure
         , 0    /* mask */
         , txCallback
         , tag);
 }
 
-bool TC6_ReadModifyWriteRegister(TC6_t *g, uint32_t addr, uint32_t value, uint32_t mask, TC6_RegCallback_t modifyCallback, void *tag)
+bool TC6_ReadModifyWriteRegister(TC6_t *g, uint32_t addr, uint32_t value, uint32_t mask, bool secure, TC6_RegCallback_t modifyCallback, void *tag)
 {
     return accessRegisters(g, REGISTER_OP_READWRITE_STAGE1, addr
         , value
-        , true /* protected */
+        , secure
         , mask
         , modifyCallback
         , tag);
@@ -598,7 +597,7 @@ static void modifyCallback(TC6_t *g, struct register_operation *reg_op, bool suc
         val = (val & ~reg_op->modifyMask) | reg_op->modifyValue;
         if (!accessRegisters(g, REGISTER_OP_READWRITE_STAGE2, addr
                             , val
-                            , true /* protected */
+                            , reg_op->secure
                             , 0    /* mask */
                             , reg_op->callback
                             , reg_op->tag)) {
@@ -966,14 +965,14 @@ static uint16_t process_tx(TC6_t *g, uint8_t *tx_buf)
 
         memcpy(&tx_buf[TC6_HEADER_SIZE], &entry->ptr[g->prot.offsetEth], tocopy_len);
         if (0u != padded_len) {
-            (void)memset(&tx_buf[TC6_HEADER_SIZE + tocopy_len], 0xCC, padded_len);
+		(void)memset(&tx_buf[TC6_HEADER_SIZE + tocopy_len], 0x0, padded_len);
         }
         g->prot.offsetEth += tocopy_len;
         TC6_ASSERT(g->prot.offsetEth <= entry->len);
         if (g->prot.offsetEth == entry->len) {
             if (entry->len < TC6_CHUNK_SIZE) {
                 /* Test hack to round up tiny Ethernet frames to full chunk size. */
-                header |= MK_MASK(HDR_EV, 1u) | MK_MASK(HDR_EBO, TC6_CHUNK_SIZE - 1u);
+		header |= MK_MASK(HDR_EV, 1u) | MK_MASK(HDR_EBO, TC6_CHUNK_SIZE - 5u);
             } else {
                 header |= MK_MASK(HDR_EV, 1u) | MK_MASK(HDR_EBO, tocopy_len - 1u);
             }
diff --git a/drivers/net/ethernet/microchip/lan865x/tc6.h b/drivers/net/ethernet/microchip/lan865x/tc6.h
old mode 100644
new mode 100755
index fab788e96037..3d1f1492720a
--- a/drivers/net/ethernet/microchip/lan865x/tc6.h
+++ b/drivers/net/ethernet/microchip/lan865x/tc6.h
@@ -156,32 +156,35 @@ bool TC6_SendRawEthernetPacket(TC6_t *pInst, const uint8_t *pTx, uint16_t len, T
 /** \brief Reads from MAC / Phy registers
  *  \param pInst - The pointer returned by TC6_Init.
  *  \param addr - The 32 Bit register offset.
+ *  \param secure - true, if TC6 secure control communication shall be used. false, otherwise.
  *  \param rxCallback - Pointer to a callback handler. May left NULL.
  *  \param pTag - Any pointer. Will be given back in given rxCallback. May left NULL.
  *  \return true, on success. false, otherwise.
  */
-bool TC6_ReadRegister(TC6_t *pInst, uint32_t addr, TC6_RegCallback_t rxCallback, void *pTag);
+bool TC6_ReadRegister(TC6_t *pInst, uint32_t addr, bool secure, TC6_RegCallback_t rxCallback, void *pTag);
 
 /** \brief Writes to MAC / Phy registers
  *  \param pInst - The pointer returned by TC6_Init.
  *  \param addr - The 32 Bit register offset.
  *  \param value - The new 32 Bit register value.
+ *  \param secure - true, if TC6 secure control communication shall be used. false, otherwise.
  *  \param txCallback - Pointer to a callback handler. May left NULL.
  *  \param pTag - Any pointer. Will be given back in given txCallback. May left NULL.
  *  \return true, on success. false, otherwise.
  */
-bool TC6_WriteRegister(TC6_t *pInst, uint32_t addr, uint32_t value, TC6_RegCallback_t txCallback, void *pTag);
+bool TC6_WriteRegister(TC6_t *pInst, uint32_t addr, uint32_t value, bool secure, TC6_RegCallback_t txCallback, void *pTag);
 
 /** \brief Reads, modifies and writes back the changed value to MAC / Phy registers
  *  \param pInst - The pointer returned by TC6_Init.
  *  \param addr - The 32 Bit register offset.
  *  \param value - The 32 Bit register bit values to be changed. This value will be set to register only if mask on the corresponding position is set to 1.
  *  \param mask - The 32 Bit register bit mask. Only Bits set to 1 will be changed accordingly to value.
+ *  \param secure - true, if TC6 secure control communication shall be used. false, otherwise.
  *  \param modifyCallback - Pointer to a callback handler. May left NULL.
  *  \param pTag - Any pointer. Will be given back in given modifyCallback. May left NULL.
  *  \return true, on success. false, otherwise.
  */
-bool TC6_ReadModifyWriteRegister(TC6_t *pInst, uint32_t addr, uint32_t value, uint32_t mask, TC6_RegCallback_t modifyCallback, void *pTag);
+bool TC6_ReadModifyWriteRegister(TC6_t *pInst, uint32_t addr, uint32_t value, uint32_t mask, bool secure, TC6_RegCallback_t modifyCallback, void *pTag);
 
 /** \brief Execute a list of register commands
  *  \param pInst - The pointer returned by TC6_Init.
-- 
2.17.1

